from setuptools import find_packages, setup

setup(name='personal-python-package',
      packages=find_packages(),
      version='0.1.0',
      description='Personal package for Click app',
      author='jraviotta',
      license='MIT',
      install_requires=['Click'])
